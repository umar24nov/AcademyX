import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTypescript from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTypescript,
  {
    // Rules newly enabled by eslint-plugin-react-hooks v7 (bundled with
    // eslint-config-next 16). They flag pre-existing, deliberate patterns
    // (prop-to-state sync, localStorage hydration, WebRTC ref caching).
    // Can be re-enabled and adopted incrementally.
    rules: {
      "react-hooks/set-state-in-effect": "off",
      "react-hooks/refs": "off",
      "react-hooks/purity": "off",
    },
  },
  globalIgnores([
    "node_modules/**",
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
